<div class="beers-foreach">
    <h1>BEERS</h1>
    <% allBeers.forEach(beer => { %>
    <img src="<%=beer.image_url%>"/>
    <h3><%=beer.name%></h3>
    <h4><%=beer.tagline%></h4>
    <p><%=beer.description%></p>
    <% }) %>
</div>